# third_party

git submodules — upstream benchmark and agent repos vendored in this tree.

```bash
git submodule update --init --recursive
```

see [docs/eval_dependencies.md](../docs/eval_dependencies.md) for links and pip extras.
