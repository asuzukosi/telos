# core (matches pyproject [project] dependencies)
tokenizers>=0.15
transformers>=4.43
huggingface_hub

# training / eval / data pipelines (install as needed)
# pip install -e ".[train,eval,data,data-gen]"

# development (matches pyproject optional-dependencies dev)
pytest>=7
